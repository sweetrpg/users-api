---
apiVersion: v1
kind: ConfigMap

metadata:
  name: api-env

data:
  ENV: local
  PORT: "8080"
  # This service's own dedicated Redis instance - see the dev overlay's comment.
  REDIS_HOST: redis.sweetrpg-users.svc.cluster.local
  REDIS_PORT: "6379"
  BUILD_INFO_PATH: /app/config/build-info.json
  LOG_LEVEL: info
